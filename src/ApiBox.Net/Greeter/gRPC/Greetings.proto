syntax = "proto3";

option csharp_namespace = "ApiBox.Net.Greeter.gRPC";

service Greetings {
	rpc Greet(GreetingRequest) returns (GreetingReply);
}

message GreetingRequest {
	string name = 1;
}

message GreetingReply {
	string content = 1;
}
